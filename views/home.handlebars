
{{#if posts.length}}
  {{#each posts}}
    <div>
      <h2>{{title}}</h2>
      <p>{{content}}</p>
      <p>Posted by {{username}} on {{createdAt}}</p>
      <a href="/post/{{id}}">Read more</a>
    </div>
  {{/each}}
{{else}}
  <p>{{message}}</p>
{{/if}}